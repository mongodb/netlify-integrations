config:
  name: git-changed-files-extension
  description: integration that prints the changed files from git when a pr is created
  slug: 76v99lm8-git-changed-files-extension
  scopes:
    site:
      - read
      - write
    env:
      - read
    user:
      - read
  integrationLevel: site