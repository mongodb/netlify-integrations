# yaml-language-server: $schema=https://sdk.netlify.com/sdk/config-schema.json
config:
  use_cjs_shims: true
  slug: netlify-search-integration
  name: netlify-search-integration
  description:  Generate and upload search manifests to Atlas from an AST
  integrationLevel: team-and-site
  scopes:
    site: ["read", "write"]
    env: 
      - read
    user: 
      - read
