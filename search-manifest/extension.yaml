# yaml-language-server: $schema=https://sdk.netlify.com/sdk/config-schema.json
config:
  use_cjs_shims: true
  slug: netlify-search-extension
  name: netlify-search-extension
  description:  Generate and upload search manifests to Atlas from an AST
  scopes:
    site: ["read", "write"]
    env: 
      - read
    user: 
      - read
